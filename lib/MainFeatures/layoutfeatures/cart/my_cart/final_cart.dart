import 'package:flutter/material.dart';

class FinalCart extends StatelessWidget {
  const FinalCart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
